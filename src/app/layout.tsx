import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Whitetail Management LLC | Land Clearing, Forestry Mulching & Site Preparation",
  description:
    "Professional land clearing, forestry mulching, grading, site preparation, and demolition services. Whitetail Management LLC transforms raw land into build-ready sites with precision and reliability.",
  keywords:
    "land clearing, forestry mulching, site preparation, grading contractors, demolition services, excavation services, land management",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
